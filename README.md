# Voice Nation CRM — Real Frontend Redesign

This build replaces the legacy dashboard UI with a standalone responsive operations console.
Run: npm.cmd install then node server.js
Open http://localhost:5000
